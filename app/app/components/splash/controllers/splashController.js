'use strict';

angular.module('groot.controllers')
  .controller('splashController', ['$scope', '$state', '$stateParams','localStorageService',
    function($scope, $state, $stateParams,localStorageService) {
      function init() {
      }
    init();
    }]);
