'use strict';

angular.module('groot.controllers')
  .controller('projectsController', ['$scope', '$state', '$stateParams','$timeout','localStorageService',
    function($scope, $state, $stateParams,$timeout,localStorageService) {
      function init() {
      }

      init();
    }
  ]);
