'use strict';

angular.module('groot.controllers')
  .controller('historyController', ['$scope', '$state', '$stateParams','localStorageService',
    function($scope, $state, $stateParams,localStorageService) {
      function init() {
      }

      init();
    }
  ]);
